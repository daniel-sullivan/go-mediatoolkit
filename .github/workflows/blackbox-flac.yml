name: flac blackbox

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  flac:
    uses: ./.github/workflows/blackbox-reusable.yml
    with:
      codec: flac
      cache_paths: /tmp/flac-upstream
