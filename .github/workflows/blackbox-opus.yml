name: opus blackbox

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  opus:
    uses: ./.github/workflows/blackbox-reusable.yml
    with:
      codec: opus
      cache_paths: /tmp/opus-upstream
